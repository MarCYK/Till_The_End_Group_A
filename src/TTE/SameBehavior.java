/*
package TTE;
public class SameBehavior {
    //To check if attack, crit and block is successful or not
    public boolean chance(double x){
        double r = Math.random()*100;
        return r >= (1 - x);
    }
    
    
    
}
*/

//still in progress, will make it once done Game_Logic